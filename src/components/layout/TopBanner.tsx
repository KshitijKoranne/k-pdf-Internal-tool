'use client';

import React from 'react';

export function TopBanner() {
  return (
    <div className="w-full bg-[hsl(var(--color-primary))] text-white text-xs font-medium py-1.5 px-4 text-center z-[60] relative">
      K-PDF | Internal Use Only
    </div>
  );
}
